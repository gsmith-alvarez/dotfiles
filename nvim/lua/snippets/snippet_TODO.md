1. Create a non greedy match for the snippets such that
    (.-) is a non-greedy match for anything (including nothing). 
    To solve the issue that currently if you are on a new line and try and use some of the math expansion it will not work.

2. Fix it so that the math is not so annoying
    Maybe make it so the math ones only work inside a markdown doc. Update it from markdown to mathdown
